//ques 1
#include <iostream>
using namespace std;

int main() {
    int arr[200], size = 0, menuChoice, i, newValue, position, foundFlag;

    while (true) {
        cout << "\n1. Create  2. Display  3. Insert  4. Delete  5. Linear Search  6. Exit\n";
        cin >> menuChoice;

        if (menuChoice == 1) {
            cout << "Enter size: ";
            cin >> size;
            for (i = 0; i < size; i++) cin >> arr[i];
        }
        else if (menuChoice == 2) {
            for (i = 0; i < size; i++) cout << arr[i] << " ";
        }
        else if (menuChoice == 3) {
            cout << "Enter value and position: ";
            cin >> newValue >> position;
            for (i = size; i > position; i--) arr[i] = arr[i - 1];
            arr[position] = newValue;
            size++;
        }
        else if (menuChoice == 4) {
            cout << "Enter position: ";
            cin >> position;
            for (i = position; i < size - 1; i++) arr[i] = arr[i + 1];
            size--;
        }
        else if (menuChoice == 5) {
            cout << "Enter value to search: ";
            cin >> newValue;
            foundFlag = 0;
            for (i = 0; i < size; i++) {
                if (arr[i] == newValue) {
                    cout << "Found at position " << i << "\n";
                    foundFlag = 1;
                    break;
                }
            }
            if (!foundFlag) cout << "Not found\n";
        }
        else if (menuChoice == 6) {
            break;
        }
    }
}
//ques 2
#include <iostream>
using namespace std;
int main(){
    int sack[90], total, a, b, c;
    cin>>total;
    for(a=0; a<total; a++) cin>>sack[a];
    for(a=0; a<total; a++){
        for(b=a+1; b<total; b++){
            if(sack[a]==sack[b]){
                for(c=b; c<total-1; c++) sack[c]=sack[c+1];
                total--; b--;
            }
        }
    }
    for(a=0; a<total; a++) cout<<sack[a]<<" ";
}

//ques 3
#include <iostream>
using namespace std;

int main() {
    int numbers[50], size, i, temp;
    
    cin >> size;
    for (i = 0; i < size; i++) cin >> numbers[i];
    
    for (i = 0; i < size / 2; i++) {
        temp = numbers[i];
        numbers[i] = numbers[size - 1 - i];
        numbers[size - 1 - i] = temp;
    }
    
    for (i = 0; i < size; i++) cout << numbers[i] << " ";
}
//ques 4
#include <iostream>
using namespace std;
int main(){
    int rA, cA, rB, cB, p, q, r;
    int matA[20][20], matB[20][20], res[20][20];
    cin>>rA>>cA;
    for(p=0; p<rA; p++) for(q=0; q<cA; q++) cin>>matA[p][q];
    cin>>rB>>cB;
    for(p=0; p<rB; p++) for(q=0; q<cB; q++) cin>>matB[p][q];
    if(cA!=rB) return 0;
    for(p=0; p<rA; p++){
        for(q=0; q<cB; q++){
            res[p][q]=0;
            for(r=0; r<cA; r++) res[p][q]+=matA[p][r]*matB[r][q];
        }
    }
    for(p=0; p<rA; p++){ for(q=0; q<cB; q++) cout<<res[p][q]<<" "; cout<<"\n";}
}
//ques 5
#include <iostream>
using namespace std;
int main(){
    int rr, cc, i2, j2, mat[25][25];
    cin>>rr>>cc;
    for(i2=0; i2<rr; i2++) for(j2=0; j2<cc; j2++) cin>>mat[i2][j2];
    for(i2=0; i2<cc; i2++){ for(j2=0; j2<rr; j2++) cout<<mat[j2][i2]<<" "; cout<<"\n";}
}
//ques 6
#include <iostream>
using namespace std;
int main(){
    int ro, co, i3, j3, tab[30][30], sumz;
    cin>>ro>>co;
    for(i3=0; i3<ro; i3++) for(j3=0; j3<co; j3++) cin>>tab[i3][j3];
    for(i3=0; i3<ro; i3++){ sumz=0; for(j3=0; j3<co; j3++) sumz+=tab[i3][j3]; cout<<"row"<<i3<<"="<<sumz<<"\n"; }
    for(j3=0; j3<co; j3++){ sumz=0; for(i3=0; i3<ro; i3++) sumz+=tab[i3][j3]; cout<<"col"<<j3<<"="<<sumz<<"\n";}
    return 0;
}